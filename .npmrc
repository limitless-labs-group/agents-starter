# Quiet, reproducible installs — the funding banner and the audit summary are
# noise on a fresh clone (the install.sh installer passes these as flags too;
# this makes a plain `npm install` quiet to match). Run `npm audit` manually
# when you actually want a vulnerability report; Dependabot tracks them on the
# repo regardless.
fund=false
audit=false
